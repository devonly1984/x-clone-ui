import UploadImage from "../shared/UploadImage";

const PostInfo = () => {
  return (
    <div className="cursor-pointer size-4 relative">
      <UploadImage src="icons/infoMore.svg" alt="" w={16} h={16} />
    </div>
  );
};
export default PostInfo;